import { useCallback } from "react";
import { Pos } from "../types";


// you must work through part 1 AND part 2 tutorials to 
// get to this point.  alternatively you can pull this branch
// and start following along now!
const useWorkerInvoker = (points: Pos[]) => {
    return useCallback(() => {


    }, [points.length]);
}

export default useWorkerInvoker;
